export type TooltipType = {
    popupDisplay: boolean    // флаг отображения подсказки
    id: number               // код региона (key в RUSSIAN_MAP)
    x?: number               // позиционирование top всплывающей подксказки
    y?: number               // позиционирование left всплывающей подксказки
}
